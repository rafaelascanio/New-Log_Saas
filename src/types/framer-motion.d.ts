declare module "framer-motion" {
  // Minimal surface so TS is happy in typecheck-only environments
  export const motion: any;
  export const AnimatePresence: any;
  const _default: any;
  export default _default;
}
