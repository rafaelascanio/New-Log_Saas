# Public read-only blob base URL used on the client
NEXT_PUBLIC_BLOB_URL_BASE=https://tibogyxm06rr5a0w.public.blob.vercel-storage.com

# Server-only secrets (left intentionally blank here)
BLOB_URL_BASE=
BLOB_READ_WRITE_TOKEN=

# Optional integrations (provide values in deployment secrets, not in repo)
SENTRY_DSN=
NEXT_PUBLIC_SENTRY_DSN=
DATA_SOURCE_URL=
